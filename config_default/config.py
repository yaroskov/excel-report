from config.lib import paths, services, unique, pseudos

# User's configuration of program.

# Options:
options = {
	"usePseudo": True,
	"useServices": True,
	"useUnique": True
}

# Paths to program files and folders:
paths = paths.data

# List of pseudonyms:
pseudos = pseudos.data

# List of services:
services = services.data

# Uniqueization dictionary:
unique = unique
