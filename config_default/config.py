from config.lib import paths, services, dict

# User's configuration of program.

# Options:
options = {
	"useServices": True,
	"useDict": True
}

# Paths to program files and folders:
paths = paths.data

# List of services:
services = services.data

# Uniqueization dictionary:
unique = dict
